# taller_01

A new Flutter project.
pasos para el api 
creas una carpeta llamada assets y dentro una data 
copias el json y guardas
entras a pubspect.yaml y linqueas la direccion de la carpeta con el json
te diriges a la ventana donde quieres poner el api
nesesitamos traer los datos con una funcion llamada list jsonLocal{}(
    final jsonString = defaultAssetBundle.of(context).loadString("asset/data/crash.json");
    con esto accede a la base de datos local y usas el api

)

