name: taller_01
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk:  ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
    #http: ^0.15.0 # Dependencia para realizar peticiones HTTP

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true


